# TODO - auto-commit

- [ ] Criar/validar script `auto-commit.sh` (commit atômico por arquivo, Conventional Commits em pt-BR, body em markdown).
- [ ] Rodar `./auto-commit.sh --no-push` para verificar commits locais.
- [ ] Rodar `./auto-commit.sh` para push no branch atual.
- [ ] Validar `git status` e `git log` para garantir mensagens e granularidade corretas.

