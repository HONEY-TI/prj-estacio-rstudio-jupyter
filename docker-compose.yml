services:

  # ==========================================
  # Imagem-base: rocker/rstudio + tidyverse/sparklyr
  # já compilados.
  # Só usada para gerar a imagem base.
  # ==========================================
  r-base:
    build:
      context: .
      dockerfile: Dockerfile.r-base
    image: alexfariakof/rstudio-dev-base:latest
    command: ["true"]

  